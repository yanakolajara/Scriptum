// import React from 'react';
// import ChatMessage from './ChatMessage';

// export default function ChatDisplay({ chat, loading }) {
//   return (
//     <div className='chat-container'>
//       {chat.map((msg, index) => (
//         <ChatMessage key={index} msg={msg} />
//       ))}
//     </div>
//   );
// }
